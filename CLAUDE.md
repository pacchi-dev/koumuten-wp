# koumuten-wp — プロジェクトコンテキスト

WordPress 案件応募用に制作する、**架空の工務店**のコーポレートサイト。

**このファイルは新しいセッションで最初に読まれる前提で書いている。**
経緯・決定事項・設計方針はすべてここと `docs/` に集約する。会話履歴に依存しない。

## このプロジェクトの目的

既存のポートフォリオ（`../portfolio_wordpress`）はダークテーマのエンジニア向けサイトで、
写真を使ったレイアウトが無い。本制作で証明したいのは次の 3 点。

1. 明るい配色の、一般的な企業サイトが作れること
2. 写真を主役にしたレイアウトができること
3. 非エンジニアのクライアント向けのサイトが作れること

### なぜ工務店なのか

CrowdWorks の「WordPress 制作・導入」143 案件を調査した結果、
**業種経験を明示的に応募条件にしていた唯一の業種が建設業**だった。

> 「建設業のホームページを3社程度作成された経験をお持ちの方希望です。
>  ポートフォリオの提示をお願いいたします」（約30万円の案件）

## 題材（架空）

| 項目 | 値 |
|---|---|
| 社名 | 株式会社ソラノワ工務店 |
| 業態 | 注文住宅・リノベーションを手がけるモダン系の工務店 |
| 所在地 | 〒670-0000 兵庫県姫路市ソラノワ町0-0-0（ダミー） |
| 電話 | 079-000-0000（ダミー） |

**モダン系にした理由**: 使用するフリー素材（Pexels / Unsplash）が欧米のモダン住宅中心のため。
伝統的な和風建築を題材にすると写真とコンセプトが一致しない。

**すべてダミーであることを必ず明示する（詳細は `docs/decisions.md` の「架空表記」）。**

## 前提となる人物像

本業で Go / PHP・Laravel / TypeScript / AWS / API 開発の実務経験があるエンジニア。
**PHP・プログラミング一般の初歩的な説明は不要。WordPress 固有の仕組みの説明が必要。**

Laravel との対比で説明すると理解が早い
（例: hooks ≒ イベント/ミドルウェア、テンプレート階層 ≒ 暗黙のルーティング規約）。

## 進め方の原則（重要）

WordPress 特有の実装に入る前に、毎回以下を簡潔に説明してから進める。

1. 今から何を実装するのか
2. WordPress ではなぜこの方法を使うのか
3. 実案件ではどのような場面で使われるのか

## 実サイト調査に基づく前提（推測ではなく実測値）

工務店・建設会社 68 サイトを訪問し、うち WordPress 50 件を構造解析した結果。

| 実測 | 設計への反映 |
|---|---|
| WordPress 率 74%、うちオリジナルテーマ 76%、既製テーマ 0 件 | **オリジナルテーマで作る**のが業界標準。README で弁明しない |
| Contact Form 7 が 42%（CF7系込みで過半数） | フォームは **CF7** で実装 |
| トップの `<img>` 枚数 中央値 55 枚 | 写真が主役。**画像最適化が必須要件** |
| ヘッダー内の電話番号 0% | ヘッダーは「お問い合わせ」ボタン。電話番号はフッター/CTA帯/お問い合わせページ |
| ナビ出現率 2 位が採用情報 28% | **採用情報を主要導線に置く** |

## 公開しない前提

ローカル（Docker）で完結させる。公開は受注後に判断する。
ただし将来レンタルサーバー（ロリポップ／エックスサーバー等の共用サーバー）へ
移設することを前提に、以下を守る。

- サーバー固有の機能に依存しない
- パスをハードコードしない（`home_url()` / `get_template_directory_uri()` を使う）
- README に移設手順を書く

## ディレクトリ構成

```
koumuten-wp/
├── docker-compose.yml      db / wordpress / wpcli / mailpit
├── bin/setup.sh            WP-CLI による冪等なセットアップ
├── bin/seed*.php           シード投入（wp eval-file で実行）
├── bin/seed/               シードデータ JSON と画像
├── mu-plugins/local-smtp.php   ローカルのみ SMTP を Mailpit へ
├── plugins/koumuten-core/  データ構造（CPT / タクソノミー / ACF / 会社情報設定）
├── themes/soranowa-koumuten/   表示（テンプレート / CSS / SEO）
└── docs/                   design.md / decisions.md
```

## 責務の分離

| | 置き場所 | 理由 |
|---|---|---|
| CPT・タクソノミー・ACF 定義・会社情報設定 | **プラグイン** | テーマを変えてもデータは残る必要がある |
| テンプレート・CSS・SEO 出力 | **テーマ** | 表示の責務 |

## コード品質の要件

実案件で提出できる品質を維持する。

- WordPress Coding Standards に従う
- 出力は必ずエスケープする（`esc_html()` / `esc_attr()` / `esc_url()` / `wp_kses_post()`）
- 入力は必ずサニタイズする（`sanitize_text_field()` 等）
- DB アクセスは `WP_Query` / `$wpdb->prepare()` を使う。生 SQL の文字列連結は禁止
- 不要なプラグイン依存を避ける（使ったプラグインと理由を README に列挙）
- ハードコードを減らし、管理画面から編集できるようにする

## Git 管理の方針

**WordPress 本体・uploads・DB・素材の実体は Git 管理しない。**

- 管理する: `themes/soranowa-koumuten/` `plugins/koumuten-core/` `docker-compose.yml` `bin/` `docs/`
- 素材は再配布せず、`bin/fetch-images.sh` で取得しなおせるようにする

## 完了の定義

`docs/design.md` の「完了条件」を参照。
